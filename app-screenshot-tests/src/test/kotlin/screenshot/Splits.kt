package screenshot

object Splits {
   val paparazziSplits = listOf(
      listOf("si.inova.androidarchitectureplayground.post"),
      // "si.inova.androidarchitectureplayground.user", "si.inova.androidarchitectureplayground.ui"
      // emptyList means "everything not above"
      emptyList(),
   )
}
